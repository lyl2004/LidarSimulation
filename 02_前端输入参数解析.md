# 前端输入参数解析

本文档只列当前前端页面可见、可自定义的输入参数，不包含后端隐藏参数、内部缓存参数或命令行高级参数。

## 1. 计算精度

| 前端名称 | 参数名 | 物理/业务含义 |
|---|---|---|
| 计算精度 | `precision` | 控制计算速度和精度。`fast` 适合快速演示，`medium` 折中，`high` 适合正式结果输出。 |

## 2. 仪器参数

| 前端名称 | 参数名 | 物理含义 |
|---|---|---|
| 波长 lambda | `wavelength_nm` | 激光工作波长，单位 nm。影响粒子散射效率、吸收特性和光电子换算。 |
| 峰值功率 P0 | `laser_peak_power_W` | 激光发射峰值功率，单位 W。功率越大，理论回波功率越强。 |
| 脉宽 tau | `pulse_width_s` | 激光脉冲持续时间，单位 s。影响单个距离门内接收到的能量，也影响距离分辨率。 |
| 接收半径 r | `receiver_radius_m` | 接收望远镜或接收口径半径，单位 m。半径越大，接收面积越大，回波信号越强。 |
| 光学效率 eta | `optical_efficiency` | 发射、接收、光学链路综合效率。越高表示系统损耗越小。 |

前端会根据上述仪器参数自动计算雷达系统常数：

```text
C = P0 * c * tau / 2 * receiver_area * optical_efficiency
```

该值后续进入雷达方程，不需要用户直接输入。

## 3. 接收端噪声 / SNR 参数

| 前端名称 | 参数名 | 物理含义 |
|---|---|---|
| 启用噪声模型 | `enabled` | 是否在结果中计算接收端噪声和信噪比。关闭后不计算有效 SNR。 |
| 量子效率 eta_q | `quantum_efficiency` | 探测器把入射光子转换为电子的效率。越高，同样回波功率产生的信号电子数越多。 |
| 背景光功率 | `background_power_W` | 进入接收端的环境背景光功率，单位 W。会抬高观测功率，同时增加背景光噪声。 |
| 暗电流 | `dark_current_A` | 探测器无光照时自身产生的电流，单位 A。会形成暗电流噪声。 |
| 读出噪声 | `read_noise_e` | 电子学读出链路的噪声，单位电子数 e-。影响弱信号场景下的噪声底。 |
| 平均脉冲数 | `average_pulses` | 多次脉冲平均次数。次数越多，随机噪声越低，SNR 越高。 |
| 生成带噪曲线 | `generate_noisy_curve` | 是否额外生成一条加入随机扰动后的曲线，便于观察噪声波动效果。 |
| 随机种子 | `random_seed` | 控制随机带噪曲线的可复现性。相同种子会得到相同随机扰动。 |

## 4. 大气分子参数

| 前端名称 | 参数名 | 物理含义 |
|---|---|---|
| 分子消光 alpha_mol | `alpha_mol` | 大气分子造成的消光系数，单位 m^-1。表示光在传播过程中被分子散射或吸收削弱的程度。 |
| 分子后向散射 beta_mol | `beta_mol` | 大气分子后向散射系数，单位 m^-1 sr^-1。表示分子把光散射回接收方向的能力。 |
| 分子退偏振比 delta_mol | `molecular-depol-ratio` | 分子散射引起偏振态变化的比例。用于偏振通道和退偏振结果计算。 |

## 5. 雾场景参数

适用场景：

| 场景 | 参数 key |
|---|---|
| 辐射雾 | `radiation_fog` |
| 平流雾 | `advection_fog` |

可编辑参数：

| 前端名称 | 参数名 | 物理含义 |
|---|---|---|
| 数量浓度 N0 | `n0_cm3` | 雾滴数量浓度，单位 cm^-3。浓度越高，消光和回波散射通常越强。 |
| 几何均值半径 rg | `rg_um` | 雾滴对数正态粒径分布的中心半径，单位 um。决定主要粒径尺度。 |
| 几何标准差 sigma_g | `sigma_g` | 粒径分布宽度。数值越大，粒径分布越宽。 |
| 折射率实部 n | `m_real` | 粒子折射率实部，影响散射方向、散射强度和相函数。 |
| 折射率虚部 k | `m_imag` | 粒子折射率虚部，表示吸收能力。越大吸收越强。 |

## 6. 霾场景参数

适用场景：

| 场景 | 参数 key |
|---|---|
| 城市/工业型霾 | `urban_industrial_haze` |
| 乡村/大陆背景型霾 | `rural_continental_haze` |
| 沙尘型霾 | `dust_desert_haze` |
| 海洋性霾 | `maritime_haze` |

每类霾通常包含两个模态：

| 模态 | 含义 |
|---|---|
| `fine` | 细模态，小粒径气溶胶组分 |
| `coarse` | 粗模态，大粒径气溶胶组分 |

每个模态可编辑参数：

| 前端名称 | 参数名 | 物理含义 |
|---|---|---|
| 数量浓度 N0 | `n0_cm3` | 当前模态气溶胶粒子数量浓度，单位 cm^-3。 |
| 几何均值半径 rg | `rg_um` | 当前模态粒径分布的中心半径，单位 um。 |
| 几何标准差 sigma_g | `sigma_g` | 当前模态粒径分布宽度。 |
| 折射率实部 n | `m_real` | 当前模态粒子的折射率实部，影响散射强度和角分布。 |
| 折射率虚部 k | `m_imag` | 当前模态粒子的吸收项，影响消光和能量损耗。 |

## 7. 雨场景参数

适用场景：

| 场景 | 参数 key |
|---|---|
| 小雨 | `light_rain` |
| 中雨 | `moderate_rain` |
| 大雨 | `heavy_rain` |

可编辑参数：

| 前端名称 | 参数名 | 物理含义 |
|---|---|---|
| 降雨率 | `rain_rate_mm_h` | 降雨强度，单位 mm/h。数值越大，雨滴谱浓度越高，消光和回波特性越明显。 |

## 8. 前端参数写入结构

前端收集参数后，会写入：

```text
temp/lidar_1d/param_overrides.json
```

整体结构为：

```json
{
  "fog": {},
  "haze": {},
  "rain": {},
  "cli": {},
  "instrument": {
    "receiver_noise": {}
  }
}
```

其中：

| 字段 | 内容 |
|---|---|
| `fog` | 雾场景参数 |
| `haze` | 霾场景参数 |
| `rain` | 雨场景参数 |
| `cli` | 波长、分子参数、系统常数等命令行参数 |
| `instrument` | 仪器参数和接收端噪声参数 |
