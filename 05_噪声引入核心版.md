# 噪声引入核心版

本文档只说明当前项目中噪声模型涉及的文件、函数、字段，以及这些内容在哪些求解层发挥作用。

## 1. 调整的主要文件

| 文件 | 作用 |
|---|---|
| `app/demo_ui.py` | 增加前端噪声参数输入、收集噪声参数、展示 SNR 曲线和摘要 |
| `temp/lidar_1d/lidar_1d_simulation.py` | 增加噪声模型、噪声计算、SNR 计算、结果写入 |
| `src/cache_keys.py` | 将噪声参数纳入结果身份 hash，避免噪声参数变化后误用旧结果 |

## 2. 前端层新增内容

文件：

```text
app/demo_ui.py
```

主要函数和字段：

| 内容 | 作用 |
|---|---|
| `_build_noise_editor()` | 在仪器参数面板中增加“接收端噪声 / SNR”参数输入 |
| `_collect_overrides()` | 收集前端噪声参数，写入 `instrument.receiver_noise` |
| `_FIELD_LABELS` 中的噪声字段 | 为噪声参数提供中文显示名 |
| `_DEFAULTS["noise"]` | 提供前端恢复默认值时使用的噪声默认参数 |
| `fig_all_snr()` | 读取 CSV 中的 SNR 数据并绘制全场景 SNR 曲线 |
| `render_summary_table(..., show_snr=True)` | 在数值摘要表中展示 `SNR@1km`、`R(SNR>=3)`、`R(SNR>=10)` |

新增或使用的前端噪声字段：

| 字段 | 含义 |
|---|---|
| `enabled` | 是否启用噪声模型 |
| `quantum_efficiency` | 量子效率 |
| `background_power_W` | 背景光功率 |
| `dark_current_A` | 暗电流 |
| `read_noise_e` | 读出噪声 |
| `average_pulses` | 平均脉冲数 |
| `generate_noisy_curve` | 是否生成随机带噪曲线 |
| `random_seed` | 随机种子 |

前端层作用：

```text
用户输入噪声参数
-> _collect_overrides()
-> param_overrides.json
```

## 3. 核心计算层新增内容

文件：

```text
temp/lidar_1d/lidar_1d_simulation.py
```

主要函数和字段：

| 内容 | 作用 |
|---|---|
| `DEFAULT_NOISE_MODEL` | 定义噪声模型默认值 |
| `NoiseModel` | 定义噪声模型数据结构 |
| `resolve_noise_model()` | 从 `param_overrides.json` 中解析噪声参数 |
| `noise_model_snapshot()` | 将噪声模型快照写入 `summary.json` |
| `compute_noise_metrics()` | 核心噪声与 SNR 计算函数 |
| `snr_curve_summary()` | 生成 SNR 摘要指标 |
| `build_effective_params_snapshot()` | 将有效噪声输入写入 `params.json` |

核心计算层作用：

```text
雷达方程得到 power_signal
-> compute_noise_metrics()
-> 得到 power_observed_raw / noise_std_power_W / noise_floor_rms_W / snr_linear / snr_db
-> snr_curve_summary()
-> 写入 summary.json 和 CSV
```

## 4. 缓存身份层新增内容

文件：

```text
src/cache_keys.py
```

主要函数和字段：

| 内容 | 作用 |
|---|---|
| `DEFAULT_NOISE_MODEL` | 与核心脚本保持一致的噪声默认值 |
| `normalize_noise_model()` | 标准化噪声参数，保证 hash 稳定 |
| `noise_hash()` | 对噪声参数生成 hash |
| `noise_hash_from_overrides()` | 从前端 overrides 中提取噪声参数并生成 hash |
| `compose_run_identity(..., noise_hash_value)` | 将噪声 hash 纳入完整运行身份 |

缓存层作用：

```text
噪声参数变化
-> noise_hash 变化
-> run identity 变化
-> 避免误用旧的 SNR / 噪声结果
```

## 5. 求解层中的作用位置

| 求解层 | 噪声内容发挥的作用 |
|---|---|
| 参数解析层 | `resolve_noise_model()` 将前端噪声输入转换为 `NoiseModel` |
| 雷达方程后处理层 | `compute_noise_metrics()` 在 `power_signal` 基础上加入背景光、暗电流、读出噪声和散粒噪声 |
| SNR 求解层 | 根据光电子数和噪声标准差计算 `snr_linear` 与 `snr_db` |
| 结果摘要层 | `snr_curve_summary()` 生成峰值 SNR、1km SNR、SNR 达标距离 |
| 输出层 | CSV 写入噪声和 SNR 曲线，`summary.json` 写入噪声模型和 SNR 摘要 |
| 缓存层 | 噪声参数进入 `noise_hash`，影响运行结果身份匹配 |
| 前端展示层 | `fig_all_snr()` 和摘要表读取 SNR 结果进行展示 |

## 6. 输出中新增或使用的字段

CSV 中主要字段：

| 字段 | 含义 |
|---|---|
| `power_observed_expected_raw` | 信号功率 + 背景光功率 |
| `power_observed_raw` | 主图使用的观测功率 |
| `power_observed_noisy_raw` | 可选随机带噪功率 |
| `noise_std_power_W` | 每个距离点的等效功率噪声标准差 |
| `noise_floor_rms_W` | RMS 噪声底 |
| `snr_linear` | 线性信噪比 |
| `snr_db` | dB 信噪比 |

`summary.json` 中主要字段：

| 字段 | 含义 |
|---|---|
| `global.noise_model` | 本次运行使用的噪声模型参数 |
| `global.noise_floor` | 全局噪声底参考值 |
| `scenario.noise` | 单场景噪声摘要 |
| `scenario.snr_summary` | 单场景 SNR 摘要 |
| `cache_identity.noise_hash` | 噪声参数 hash |

## 7. 简短总结

噪声引入主要改动集中在三处：

```text
app/demo_ui.py                 前端输入与展示
lidar_1d_simulation.py         噪声/SNR 核心计算与结果写入
src/cache_keys.py              噪声参数参与缓存身份
```

它不改变 Mie、T-matrix 或光学散射主求解，只在雷达方程得到回波功率之后，作为接收端后处理模型计算噪声底和 SNR。
